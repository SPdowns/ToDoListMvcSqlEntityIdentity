@{
  Layout = "_Layout";
}

@using ToDoListMvcSqlEntityIdentity.Models;
@model List<ToDoListMvcSqlEntityIdentity.Models.Item>;


<h1>Items</h1>

  @foreach(Item item in Model)
  {
      @Html.CheckBox("IsComplete", @item.IsComplete)  
      @Html.ActionLink($"{ item.Description}", "Details", new { id = item.ItemId })
      <br>
      @* <input type="checkbox" id="MyBooleanValue" name="MyBooleanValue" value= @item.IsComplete>
      <label for="MyBooleanValue">Finished!</label> *@
  }

<p>@Html.ActionLink("Add new item", "Create")</p>
<p>@Html.ActionLink("Home", "Index", "Home")</p>